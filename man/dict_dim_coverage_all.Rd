% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Dictionary.R
\name{dict_dim_coverage_all}
\alias{dict_dim_coverage_all}
\title{Compute coverage for all SADCAT dimensions/facets}
\usage{
dict_dim_coverage_all(
  df,
  dict_df = SADCAT::Spanishdicts,
  palabra_col = "Palabra",
  prefix = "descriptor_",
  dict_vars = NULL,
  out_prefix = "cov_",
  out_suffix = "_pct"
)
}
\arguments{
\item{df}{A data frame containing descriptor columns.}

\item{dict_df}{The SADCAT dictionary data frame (default \code{SADCAT::Spanishdicts}).}

\item{palabra_col}{Name of the token column in \code{dict_df} (default \code{"Palabra"}).}

\item{prefix}{Prefix used to identify descriptor columns in \code{df} (default \code{"descriptor_"}).}

\item{dict_vars}{Optional subset of \verb{*_dict} columns to use. If \code{NULL}, all \verb{*_dict} columns are used.}

\item{out_prefix}{Prefix for new coverage columns (default \code{"cov_"}).}

\item{out_suffix}{Suffix for new coverage columns (default \code{"_pct"}).}
}
\value{
\code{df} with additional coverage columns (one per \verb{*_dict} variable). Each value is a percentage.
}
\description{
Computes per-row coverage for each dictionary membership column ending in \code{"_dict"} (or a user-specified
subset via \code{dict_vars}). For each variable, the function reports the percentage of candidate descriptors
(non-missing and non-empty cells) that map to that variable.
}
\examples{
# Minimal example with a toy "dictionary" data frame
dict_df <- data.frame(Palabra = c("elegante", "solemne"),
                      warmth_dict = c(1L, 0L),
                      competence_dict = c(0L, 1L))
df <- data.frame(descriptor_1 = c("elegante", "solemne"),
                 descriptor_2 = c("solemne", NA))
dict_dim_coverage_all(df, dict_df = dict_df)

}
