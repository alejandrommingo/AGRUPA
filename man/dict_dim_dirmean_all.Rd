% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Dictionary.R
\name{dict_dim_dirmean_all}
\alias{dict_dim_dirmean_all}
\title{Compute mean direction scores for all SADCAT dimensions/facets}
\usage{
dict_dim_dirmean_all(
  df,
  dict_df = SADCAT::Spanishdicts,
  palabra_col = "Palabra",
  prefix = "descriptor_",
  dir_vars = NULL,
  out_prefix = "dirmean_",
  out_suffix = "",
  n_prefix = "n_dirmean_",
  strip_dir_suffix = TRUE
)
}
\arguments{
\item{df}{A data frame containing descriptor columns.}

\item{dict_df}{The SADCAT dictionary data frame (default \code{SADCAT::Spanishdicts}).}

\item{palabra_col}{Name of the token column in \code{dict_df} (default \code{"Palabra"}).}

\item{prefix}{Prefix used to identify descriptor columns in \code{df} (default \code{"descriptor_"}).}

\item{dir_vars}{Optional subset of \verb{*_dir} columns to use. If \code{NULL}, all \verb{*_dir} columns are used.}

\item{out_prefix}{Prefix for mean direction columns (default \code{"dirmean_"}).}

\item{out_suffix}{Suffix for mean direction columns (default \code{""}).}

\item{n_prefix}{Prefix for count columns (default \code{"n_dirmean_"}).}

\item{strip_dir_suffix}{Logical. If \code{TRUE}, drop the \verb{_dir} suffix in output names.}
}
\value{
\code{df} with additional mean direction and count columns.
}
\description{
For each \verb{*_dir} variable (or a user-specified subset via \code{dir_vars}), this function computes:
\itemize{
\item the mean direction score per row (based only on descriptors with non-missing direction values), and
\item the number of contributing descriptors used for that mean (\verb{n_dirmean_*}).
}
}
\details{
Duplicate entries in the dictionary are aggregated by token using a mean (ignoring missing values).
}
\examples{
# Minimal example with a toy "dictionary" data frame
dict_df <- data.frame(Palabra = c("elegante", "solemne", "solemne"),
                      warmth_dir = c( 1, NA,  3),
                      competence_dir = c(0,  2, NA))
df <- data.frame(descriptor_1 = c("elegante", "solemne"),
                 descriptor_2 = c("solemne", NA))
dict_dim_dirmean_all(df, dict_df = dict_df)

}
