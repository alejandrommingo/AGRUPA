% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Data.R
\name{prepare_descriptors}
\alias{prepare_descriptors}
\title{Prepare candidate descriptors for SADCAT lookup}
\usage{
prepare_descriptors(
  x,
  input_type = c("string", "vector", "data"),
  input_format = c("comma", "text"),
  desc_col = NULL,
  prefix = "descriptor_",
  drop_desc_col = TRUE,
  remove_diacritics = TRUE,
  keep_enye = FALSE,
  lemmatize = c("none", "lemma", "both"),
  udpipe_model = NULL,
  lemma_suffix = "_lemma",
  include_ngrams = TRUE,
  remove_stopwords = TRUE,
  stopwords_lang = c("es", "en"),
  stopwords_custom = NULL,
  max_ngrams = Inf,
  text_col = "text"
)
}
\arguments{
\item{x}{Input object (string, vector, or data frame). See \code{input_type}.}

\item{input_type}{\code{"string"}, \code{"vector"}, or \code{"data"}.}

\item{input_format}{\code{"comma"} or \code{"text"}.}

\item{desc_col}{For \code{input_type = "data"}, the name of the column containing descriptors/text.}

\item{prefix}{Prefix for descriptor columns (default \code{"descriptor_"}).}

\item{drop_desc_col}{Logical. For \code{input_type = "data"}, drop \code{desc_col} from the output.}

\item{remove_diacritics}{Logical. Remove diacritics using \strong{stringi}.}

\item{keep_enye}{Logical. Preserve \code{"ñ"} when stripping other diacritics.}

\item{lemmatize}{\code{"none"}, \code{"lemma"}, or \code{"both"}.}

\item{udpipe_model}{An \code{udpipe_model} object or a path to a \code{.udpipe} file (required if lemmatizing).}

\item{lemma_suffix}{Suffix for lemma columns when \code{lemmatize = "both"} (default \code{"_lemma"}).}

\item{include_ngrams}{Logical. For \code{input_format = "text"}, include bigrams and trigrams.}

\item{remove_stopwords}{Logical. For \code{input_format = "text"}, remove stopwords for unigrams.}

\item{stopwords_lang}{\code{"es"} or \code{"en"}. Language used for stopword lists (stopwords-iso).}

\item{stopwords_custom}{Optional character vector of custom stopwords.}

\item{max_ngrams}{Maximum number of bigrams and trigrams per row (default \code{Inf}).}

\item{text_col}{Name of the column that holds the original text in the output (default \code{"text"}).}
}
\value{
A data frame with one row per input row. Descriptor columns are in wide format.
}
\description{
High-level wrapper that extracts and normalizes candidate descriptors either from:
\itemize{
\item comma-separated descriptor strings (\code{input_format = "comma"}), or
\item narrative text (\code{input_format = "text"}) as unigrams plus optional bigrams/trigrams.
}
}
\details{
The output is always a \strong{wide} data frame with columns starting with \code{prefix} (by default
\code{descriptor_}), ready to be consumed by \code{dict_coverage()}, \code{dict_dim_coverage_all()},
and \code{dict_dim_dirmean_all()}.
}
\section{Normalization}{

The pipeline mirrors the format used by the AGRUPA/SADCAT lookup tables:
\itemize{
\item lowercase,
\item remove \emph{all} whitespace (including internal spaces),
\item optionally strip diacritics (accent removal),
\item optionally keep \code{"ñ"} untouched.
}
}

\section{Lemmatization}{

If \code{lemmatize} is \code{"lemma"} or \code{"both"}, UDPipe is used. For narrative text, lemmatization
is run on the full document to preserve context (UDPipe annotation), then tokens/lemmas are
normalized. With \code{lemmatize = "both"}, lemma columns are included only when they add
information (lemmas identical to surface forms are set to \code{NA}) and deduplicated within-row.
}

\section{N-grams}{

For \code{input_format = "text"}, bigrams and trigrams are generated \emph{within punctuation-delimited segments},
preventing n-grams from crossing punctuation boundaries. N-grams are normalized by removing whitespace,
so \code{"alta calidad"} becomes \code{"altacalidad"}.
}

\examples{
# Comma-separated descriptors:
prepare_descriptors("elegante, solemne", input_type = "string", input_format = "comma")

# Narrative text:
prepare_descriptors("Una obra magistral y compleja.", input_type = "string", input_format = "text")

}
