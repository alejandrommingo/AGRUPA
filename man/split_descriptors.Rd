% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Data.R
\name{split_descriptors}
\alias{split_descriptors}
\title{Split and normalize comma-separated descriptors (legacy helper)}
\usage{
split_descriptors(
  x,
  input_type = c("string", "vector", "data"),
  desc_col = NULL,
  prefix = "descriptor_",
  drop_desc_col = TRUE,
  remove_diacritics = TRUE,
  keep_enye = FALSE,
  lemmatize = c("none", "lemma", "both"),
  udpipe_model = NULL,
  lemma_suffix = "_lemma"
)
}
\arguments{
\item{x}{Input descriptors. See \code{input_type}.}

\item{input_type}{\code{"string"}, \code{"vector"}, or \code{"data"}. Controls how \code{x} is interpreted.}

\item{desc_col}{For \code{input_type = "data"}, the name of the column in \code{x} that contains the
comma-separated descriptors.}

\item{prefix}{Prefix for generated descriptor columns (default \code{"descriptor_"}).}

\item{drop_desc_col}{Logical. For \code{input_type = "data"}, drop \code{desc_col} from the output.}

\item{remove_diacritics}{Logical. If \code{TRUE}, remove accents and diacritics using \strong{stringi}.}

\item{keep_enye}{Logical. If \code{TRUE}, preserve \code{"ñ"} when removing other diacritics.}

\item{lemmatize}{\code{"none"}, \code{"lemma"}, or \code{"both"}.}

\item{udpipe_model}{An \code{udpipe_model} object or a path to a \code{.udpipe} file (required if lemmatizing).}

\item{lemma_suffix}{Suffix for lemma columns when \code{lemmatize = "both"} (default \code{"_lemma"}).}
}
\value{
A data frame with one row per input row and descriptor columns in wide format.
}
\description{
This function takes a comma-separated descriptor field and converts it into a \strong{wide} data frame
with columns \code{descriptor_1}, \code{descriptor_2}, ... suitable for dictionary lookup.
}
\details{
It performs the AGRUPA/SADCAT normalization steps:
\itemize{
\item trimming,
\item lowercasing,
\item removal of \emph{all} whitespace,
\item optional diacritic stripping (accent removal),
\item optional UDPipe lemmatization (\code{lemmatize = "lemma"} or \code{"both"}).
}

This function is kept for backwards compatibility. For new code, prefer
\code{\link[=prepare_descriptors]{prepare_descriptors()}} which supports both comma-separated descriptors and narrative text
(unigrams and n-grams).

When \code{lemmatize = "both"}, lemma-based candidates are returned in additional columns with
suffix \code{lemma_suffix}, but only when the lemma adds new information (lemmas identical to
the normalized surface form are set to \code{NA} to avoid double counting).
}
\examples{
split_descriptors("elegante, solemne", input_type = "string")

}
